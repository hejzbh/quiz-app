export const createQuizzURL = () => "/create-quizz";
export const quizzDetailsURL = (quizzID: string) => `/edit-quizz/${quizzID}`;
export const playQuizzURL = (quizzID: string) => `/play-quizz/${quizzID}`;
