export enum APIEndpoints {
  getQuizzes = "/quizzes",
  postQuizz = "/quizzes",
  editQuizz = "/quizzes/",
  deleteQuiz = "/quizzes/",
}

export enum APIMethods {
  GET = "GET",
  DELETE = "DELETE",
  POST = "POST",
  EDIT = "PUT",
}

export enum ButtonTypes {
  LINK = "LINK",
  BUTTON = "BUTTON",
}
